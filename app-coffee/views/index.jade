doctype html
head
  meta(charset='utf-8')
  title http://coffee
  meta(name="viewport", content="width=device-width, initial-scale=1.0")

  != css('app')
  
body
  section#temperature-chart
  != js('lib/jquery')
  != js('lib/highcharts')
  != js('app')
